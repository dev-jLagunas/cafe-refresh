import { Routes } from '@angular/router';
import { LandingHomeComponent } from './landing-home/landing-home.component';

export const routes: Routes = [{ path: '', component: LandingHomeComponent }];
